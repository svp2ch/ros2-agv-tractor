# Параметры для Gazebo сервера
# Файл для справки — основные параметры передаются через launch-аргументы

gazebo:
  ros__parameters:
    # Использовать системное время ROS вместо симуляционного
    use_sim_time: true
